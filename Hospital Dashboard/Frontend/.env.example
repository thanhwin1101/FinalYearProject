# Backend API URL (optional - uses proxy in development)
# Only needed for production builds
VITE_API_URL=

# For production, set this to your backend URL:
# VITE_API_URL=http://your-backend-server:3000
